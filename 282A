#include <iostream>
#include<string>
using namespace std;

int main()
{
int n,X;
string a;
cin>>n;
X=0;
for(int i=1;i<=n;i++){
    cin>>a;
    if(a == "++X" || a=="X++")
        {
        X++;
    }
    else if(a== "--X"|| a== "X--")
    {
            --X;
    }
}
 cout<<X<<endl;
}
